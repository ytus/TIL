## Webstorm `/usr/bin/env: ‘node’: No such file or directory`

When running a node package as an _External Tool_ in Webstorm, you can get this error:

> /usr/bin/env: ‘node’: No such file or directory

To fix it ... TODO ???
